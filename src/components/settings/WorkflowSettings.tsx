/**
 * Workflow Settings Re-export
 * ===========================
 *
 * Re-exports the modular workflow settings component.
 * Maintains backward compatibility with existing imports.
 *
 * @module components/settings/WorkflowSettings
 * @see components/settings/workflow
 */

export { WorkflowSettings, default } from './workflow';
